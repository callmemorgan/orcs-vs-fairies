services:
  orcs-vs-fairies:
    image: "[[ORCS_V_FAIRIES_IMAGE]]"
    restart: unless-stopped
    ports:
      - "127.0.0.1:8088:8088"
    read_only: true
    tmpfs:
      - /var/cache/nginx
      - /var/run
      - /tmp
